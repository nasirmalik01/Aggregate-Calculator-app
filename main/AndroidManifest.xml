<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.whatsapp.hybrid.aggregatecalculationcatlculator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".secondActivity" />
        <activity android:name=".thirdActivity" />
        <activity android:name=".fourth" />
        <activity android:name=".pieasActivity" />
        <activity android:name=".giki" />
        <activity android:name=".FrontActivity"></activity>
        <activity android:name=".MedicalfirstActivity" />
        <activity android:name=".uhscalculation" />
        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NUMS"></activity>
    </application>

</manifest>